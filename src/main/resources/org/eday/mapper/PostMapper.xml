<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.eday.mapper.PostMapper">

	<insert id="insertPost">
		INSERT INTO Post( detail, regdate, member_id) VALUES( #{detail}, now(), #{member_id})
	</insert>
	
	<insert id="insertPhoto">
		insert into post_photo(post_id, upload_path, photo_name)
		values((select max(post_id) from post), #{upload_path}, #{photo_name})
	</insert>
	
	<delete id="deletePhoto">
		delete from post_photo where post_name = #{post_name}
	</delete>
	
	<select id="findByPostPostId">
		select * from post_photo where post_id = #{post_id}
	</select>
	
	<select id="searchHashtag" resultType="org.eday.domain.HashtagVO">
		SELECT * FROM Hashtag
		WHERE hashtag||keyword like '%'||#{keyword}||'%'
	</select>

</mapper>